const LoadingSpinner = () => {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <div className="size-20 animate-spin rounded-full border-4 border-dotted border-blue-500"></div>
    </div>
  );
};

export default LoadingSpinner;
